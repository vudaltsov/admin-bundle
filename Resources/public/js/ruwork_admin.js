'use strict';

(function ($) {
    //var $document = $(document)

    $.extend($.fn.markdown.defaults, {
        iconlibrary: 'fa',
        hiddenButtons: ['cmdImage']
        //language: app.locale
    })
})(window.jQuery)

//
